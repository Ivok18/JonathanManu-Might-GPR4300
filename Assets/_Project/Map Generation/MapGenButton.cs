using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

namespace Might.MapGeneration
{
   /* [CustomEditor(typeof(GenerationBehaviour))]
    public class MapGenButton : Editor
    {
        public override void OnInspectorGUI()
        {
            DrawDefaultInspector();

            GenerationBehaviour targetSpript = (GenerationBehaviour)target;

            if(GUILayout.Button("Generate Caves"))
            {
                targetSpript.Generation();
            }
        }
    }*/
}
